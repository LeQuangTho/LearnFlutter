// String constants
// ignore_for_file: non_constant_identifier_names

const String MODE_DEV = 'DEV';
const String MODE_PRODUCTION = 'PRODUCTION';
final DateTime DATE_TIME_NOW = DateTime.now();

// Time constants
const int LIMIT_RESPONSE_TIME = 200;
const int OTP_AVAILABLE_DURATION = 30;
const Duration DELAY_100_MS = Duration(milliseconds: 100);

const Duration DELAY_250_MS = Duration(milliseconds: 250);
const Duration DELAY_500_MS = Duration(milliseconds: 500);
const Duration DELAY_1_S = Duration(seconds: 1);

// API methods
const POST = 'POST';
const GET = 'GET';
const PUT = 'PUT';
const DELETE = 'DELETE';
const PATCH = 'PATCH';

// Biometric
const CURRENT_USER = 'CURRENT_USER';
