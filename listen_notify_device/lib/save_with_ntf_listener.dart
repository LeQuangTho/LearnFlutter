import 'package:flutter/material.dart';

class SaveWithNTFListener extends StatelessWidget {
  const SaveWithNTFListener({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
